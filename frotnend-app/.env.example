# Backend API URL (Optional - users can configure via login screen)
# Leave as localhost for desktop development
# For mobile, users will enter the server IP in the app login screen
VITE_API_URL=http://localhost:8080

# Google OAuth redirect URL (desktop only)
GOOGLE_REDIRECT_URL=http://localhost:8080/callback
